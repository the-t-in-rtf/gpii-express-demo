This is a demo of the `gpii.express` component.  To run the test server, use a command like `node index.js`.

The configured port number should be displayed on screen, open that in a browser, and, hey presto, you're there.